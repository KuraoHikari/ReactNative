import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Auth = () => {
  return (
    <View>
      <Text>Akun</Text>
    </View>
  );
};

export default Auth;

const styles = StyleSheet.create({});
